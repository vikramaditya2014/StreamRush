Common Cloudinary unsigned preset names to try:

1. ml_default
2. unsigned_default  
3. default_unsigned
4. upload_preset
5. unsigned_uploads

Try each one in your .env file:
VITE_CLOUDINARY_UPLOAD_PRESET=preset_name_here